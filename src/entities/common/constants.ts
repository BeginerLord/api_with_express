export const enum StatusType {
    ACTIVE = "active",
    INACTIVE = "inactive",
    ARCHIVED = "archived",
    DELETED = "deleted",
  }
  
  export const enum RolType {
    USERNOTES = "usernotes",
  }